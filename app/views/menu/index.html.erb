<body>

<!-- PRELOADER -->
<div class="preloader">
    <div class="inner">
        <div class="item item1"></div>
        <div class="item item2"></div>
        <div class="item item3"></div>
    </div>
</div>
<!-- END / PRELOADER -->

<!-- PAGE WRAP -->
<div id="page-wrap">
    
    <!-- HEADER -->
        <%= render "home/header" %>
    <!-- END / HEADER -->
    
    <!-- THE MENU -->
    <section id="the-menu" class="the-menu section">
        <div class="tabs-menu tabs-page">
            <div class="container">
                <ul class="nav-tabs text-center" role="tablist">
                    <% @category_food.each do |category| %>
                        <% if category.id == @category_food.first.id %>
                            <li class="active"><a href="#<%= category.id %>" role="tab" data-toggle="tab"><%= category.name %></a></li>
                        <% else %>
                            <li><a href="#<%= category.id %>" role="tab" data-toggle="tab"><%= category.name %></a></li>
                        <% end %>
                    <% end %>
                    <li><a href="#drink" role="tab" data-toggle="tab">Drink</a></li>
                </ul>
            </div>
        </div>
        <div class="section-content">
            <div class="container">
                <div class="tab-menu-content tab-content">
                    <% @category_food.each do |category| %>
                        <% if category.id == @category_food.first.id %>
                            
                            <!-- CATEGORIES -->
                            <div class="tab-pane fade in active" id="<%= category.id %>">
                                <div class="row">
                                    <% @menu.each do |item| %>  
                                        <% if item.category_id == category.id %>
                                            
                                        <!-- THE MENU ITEM -->
                                        <div class="col-lg-12">
                                            <div class="the-menu-item">
                                                <div class="image-wrap">
                                                    <img src="<%= item.image_url.image_product_large if item.image_url? %>" width="160" height="160" class="border-product">
                                                </div>
                                                <div class="the-menu-body">
                                                    <h4 class="xsm"><%= item.name_1 %></h4>
                                                    <p>+ <%= item.name_2 %></p>
                                                    <em>+ <%= item.description %></em>
                                                    <hr/>
                                                    <div><%= button_to t(:add_to_cart), line_items_path(menu_id: item), class: "btn btn-info" %></div>
                                                </div>
                                                <div class="prices">
                                                    <span class="price xsm"><%= item.price %></span>
                                                </div>
                                                
                                            </div>
                                        </div>
                                        <!-- END / THE MENU ITEM -->
                                            
                                        <% end %>
                                    <% end %>        
                                </div>
                                
                            </div>
                            <!-- END / CATEGORIES -->
                            
                        <% else %>
                            <!-- CATEGORIES -->
                            <div class="tab-pane fade" id="<%= category.id %>">
                                <div class="row">
                                    <% @menu.each do |item| %>  
                                        <% if item.category_id == category.id %>
                                            
                                        <!-- THE MENU ITEM -->
                                        <div class="col-lg-12">
                                            <div class="the-menu-item">
                                                <div class="image-wrap">
                                                    <img src="<%= item.image_url.image_product_large if item.image_url? %>" width="160" height="160" class="border-product">
                                                </div>
                                                <div class="the-menu-body">
                                                    <h4 class="xsm"><%= item.name_1 %></h4>
                                                    <p>+ <%= item.name_2 %></p>
                                                    <em>+ <%= item.description %></em>
                                                    <hr/>
                                                    <div><%= button_to 'Add to Cart', line_items_path(menu_id: item), class: "btn btn-info" %></div>
                                                </div>
                                                <div class="prices">
                                                    <span class="price xsm"><%= item.price %></span>
                                                </div>
                                            </div>
                                        </div>
                                        <!-- END / THE MENU ITEM -->
                                            
                                        <% end %>
                                    <% end %>        
                                </div>
                                
                            </div>
                            <!-- END / CATEGORIES -->
                        <% end %>
                    <% end %>
                    
                    <!-- CATEGORIES -->
                    <div class="tab-pane fade" id="drink">
                        <div class="row">
                            <% @category_drink.each do |category| %>
                                <!-- THE MENU ITEM -->
                                <div class="col-lg-12">
                                    <h5><strong><%= category.name %></strong></h5>
                                </div>
                                <!-- END / THE MENU ITEM -->
                                 
                                <% @menu.each do |item| %>  
                                    <% if item.category_id == category.id %>
                                        
                                    <!-- THE MENU ITEM -->
                                    <div class="col-lg-12">
                                        <div class="the-menu-item">
                                            <div class="image-wrap">
                                                <img src="<%= item.image_url.image_product_large if item.image_url? %>" width="160" height="160" class="border-product">
                                            </div>
                                            <div class="the-menu-body">
                                                <h4 class="xsm"><%= item.name_1 %></h4>
                                                <p>+ <%= item.name_2 %></p>
                                                <em>+ <%= item.description %></em>
                                                <div><%= button_to 'Add to Cart', line_items_path(menu_id: item), class: "btn btn-info" %></div>
                                            </div>
                                            <div class="prices">
                                                <span class="price xsm"><%= item.price %></span>
                                            </div>
                                        </div>
                                    </div>
                                    <!-- END / THE MENU ITEM -->
                                        
                                    <% end %>
                                <% end %>
                            <% end %>
                        </div> 
                    </div>
                    <!-- END / CATEGORIES -->
                    
                </div>
            </div>
        </div>
    </section>
    <!-- END / THE MENU -->


    <!-- FOOTER -->
    <footer id="footer" class="footer">
        <div class="divider divider-1 divider-color"></div>
        <div class="awe-color"></div>
        <div class="container">
            <div class="copyright text-center">
                <%= render "home/footer" %>
            </div>
        </div>
    </footer>
    <!-- END / FOOTER -->
    

</div>
<!-- END / PAGE WRAP -->

<!-- LOAD JQUERY -->
<%= javascript_include_tag '/frontend/js/lib/jquery-1.11.2.min.js' %>

<!-- GOOGLE MAP -->
<%= javascript_include_tag 'http://maps.google.com/maps/api/js?sensor=false' %>
<%= javascript_include_tag '/frontend/js/lib/bootstrap.min.js' %>
<%= javascript_include_tag '/frontend/js/lib/jquery.bxslider.min.js' %>
<%= javascript_include_tag '/frontend/js/lib/jquery.easing.min.js' %>
<%= javascript_include_tag '/frontend/js/lib/jquery.owl.carousel.min.js' %>
<%= javascript_include_tag '/frontend/js/lib/masonry.pkgd.min.js' %>
<%= javascript_include_tag '/frontend/js/lib/perfect-scrollbar.min.js' %>
<%= javascript_include_tag '/frontend/js/lib/jquery.magnific-popup.min.js' %>
<%= javascript_include_tag '/frontend/js/lib/jquery.parallax-1.1.3.js' %>
<%= javascript_include_tag '/frontend/js/lib/retina.min.js' %>
<%= javascript_include_tag '/frontend/js/scripts.js' %>

</body>